#include <stdio.h>

int main() {
    int a, b, c, soma;

    printf("Digite tres valores: ");
    scanf("%d %d %d", &a, &b, &c);

    soma = (a * a) + (b * b) + (c * c);

    printf("A soma dos quadrados eh: %d\n", soma);

    return 0;
}
